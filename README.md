# Fundzy

This project provides a minimal donation API built with Flask and SQLAlchemy.

## Setup

1. Install the dependencies:
   ```bash
   pip install -r requirements.txt
   ```

2. Initialize the database by running the application once:
   ```bash
   python app.py
   ```
   The app will create `donations.db` automatically on the first run.

